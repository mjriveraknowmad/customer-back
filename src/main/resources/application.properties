# App config
server.port=8080
spring.application.name=BootCustomer
# server.context-path=/customer

# api-docs endpoint custom path
# springdoc.api-docs.path=/v3/api-docs
# springdoc.api.doc.enabled=false

# Disabling the swagger-ui
# springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html
# springdoc.swagger-ui.config-url: '/ingress-path/v3/api-docs/swagger-config'
# springdoc.swagger-ui.doc-expansion: none
# springdoc.swagger-ui.disable-swagger-default-url: true
# Packages to include
# Por defecto será 54321, en caso de que se haya definido la variable de entorno DB_PASSWORD en los secret, se usará su valor. Esto es útil para no tener que poner la contraseña en el código fuente, y poder cambiarla sin tener que recompilar la aplicación.
db.password= ${DB_PASSWORD:54321}